/* eslint-disable no-undef */
// eslint-disable-next-line no-undef
const MarketPlaceContract = artifacts.require("EventNftMarket");

contract("Event", accounts => {
    
    describe("Get listing price", () => {

        beforeEach(async () => {
            
        })


    })
})